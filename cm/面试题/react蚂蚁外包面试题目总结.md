### react蚂蚁外包面试题目总结

##### ***号为高频题目，但是不代表其他不重要。祝各位考运昌隆；
```
  *** 1.react对页面render异常捕获：错误边界，看官网；
      2.react-router和react-router-dom的原理及区别；
  *** 3,promise和async的区别：async可以return await的promise对象；看es6语法；
      4,null和undefined区别；typeof null  typeof undefined 结果是什么 ；== ===的结果是否相同
      5,map, filter,reduce 用法区别；
  *** 6,css垂直水平居中方式，最好能打出来3种或以上；
  *** 7，redux的整体使用流程；action，reducer能讲清楚，最好细致一点，provider，context，connect传递store的流程都能说出来；
  *** 8，react里面key的作用； 使diff算法更高效；
  *** 9，执行两次setState，render几次，会不会立即触发。答：批处理；不懂就百度；
      10，路由的两种方式hash/history 的区别；
  *** 11，箭头函数和普通函数区别；this指向，不能作为构造函数，不能访问arguments
      12，函数声明和函数赋值语句的差别；
  *** 13，虚拟dom，diff算法
  *** 14，react key的属性；提升diff的性能；具体看下diff算法；
  *** 15，react高阶组件:传入一个组件，返回一个组件；比如connect；
  *** 16，react hooks解决什么问题
  *** 17，解释useEffect 的第二个参数传不同值的区别；
      18，reducer是纯函数吗？是，状态可追溯；
      19，flex 具体属性；
      20，position的值有哪些；讲清楚；
  *** 21，promise原理，用法；哪些api；
      22，react事件和原生浏览器事件
      23，es6 Map与Object的区别；
      24，es6 static关键字编译成es5后的代码；
      25，PureComponent与Component的区别
  *** 26，事件传播流程；捕获，目标，触发；
  *** 27，hooks 知道多少 说多少。
```